apiVersion: v1
kind: ReplicationController
metadata:
  name: swarm
  labels:
    app: swarm
spec:
  replicas: 1
  selector:
    app: swarm
  template:
    metadata:
      labels:
        app: swarm
    spec:
      containers:
        - name: swarm
          image: "b.gcr.io/kuar/swarm:1.0.0"
          args:
            - "--log-level=debug"
            - "manage"
            - "--cluster-driver=kubernetes-experimental"
            - "--host=0.0.0.0:2375"
            - "127.0.0.1:8080"
          resources:
            limits:
              cpu: 100m
              memory: 50Mi
          ports:
            - containerPort: 2375
          imagePullPolicy: Always
